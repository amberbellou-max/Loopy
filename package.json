{
  "name": "loopy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && if [ -f dist/index.build.html ]; then mv dist/index.build.html dist/index.html; fi",
    "build:pages-root": "GITHUB_ACTIONS=true GITHUB_REPOSITORY=amberbellou-max/Loopy npm run build && rm -rf assets && cp -R dist/assets assets",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.13.5",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  }
}
